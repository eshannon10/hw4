<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Travel Journal</title>
  
  <!-- Bootstrap CSS via CDN -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

  <!-- Custom styles -->
  <%= stylesheet_link_tag "application", media: "all" %>
</head>
<body>

  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container">
      <a class="navbar-brand" href="<%= root_path %>">Travel Journal</a>
      <div class="collapse navbar-collapse">
        <ul class="navbar-nav ml-auto">
          <% if current_user %>
            <li class="nav-item">
              <span class="navbar-text">Hi, <%= current_user.username %></span>
            </li>
            <li class="nav-item">
              <form id="logout-form" action="<%= logout_path %>" method="POST" style="display: inline;">
                <input type="hidden" name="_method" value="delete">
                <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">
                <button type="submit" class="nav-link btn btn-link" style="border: none; padding: 0; background: none; cursor: pointer;">
                  Logout
                </button>
              </form>
            </li>
          <% else %>
            <li class="nav-item">
              <%= link_to 'Login', login_path, class: 'nav-link' %>
            </li>
            <li class="nav-item">
              <%= link_to 'Sign Up', signup_path, class: 'nav-link' %>
            </li>
          <% end %>
        </ul>
      </div>
    </div>
  </nav>

  <div class="container mt-4">
    <%= yield %>
  </div>

  <!-- ✅ Bootstrap JavaScript (Fixed) -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>
